<!-- Info Section -->
<div class="login-section">
    <div class="image-layer" style="background-image: url(https://devfinding-production.s3.ap-southeast-1.amazonaws.com/background.png);"></div>
    <div class="outer-box">
        <!-- Login Form -->
        <div class="login-form default-form">
            <div class="form-inner">
                <h3>
                    Forgot Password Form
                </h3>
                <% if flash[:forgot_password_success] %>
                    <div class="alert alert-success">
                      <a class="close" data-dismiss="alert"></a>
                      <%= flash[:forgot_password_success] %>
                    </div>
                <% end %>
                <% if flash[:can_not_find_user] %>
                    <div class="alert alert-danger">
                      <a class="close" data-dismiss="alert"></a>
                      <%= flash[:can_not_find_user] %>
                    </div>
                <% end %>
                <!--Login Form-->
                <%= form_for(:hr_freelancer, :url => { :controller => 'hr_freelancers/sessions', :action => 'forgot_password'}) do |f| %>
                    <div class="form-group">
                        <label>Your Email</label>
                        <%= f.email_field :email, :class=>"form-control", :required => true, :placeholder => 'Enter your email' %> 
                    </div>
                    <div class="form-group">
                        <button class="theme-btn btn-style-one" type="submit" name="log-in">
                          Forgot Password
                        </button>
                    </div>
                <% end %>
                <div class="bottom-box">
                    <div class="text">have an account? <a href="/hr_freelancers/sessions/signin">Signin</a></div>
                </div>
            </div>
        </div>
        <!--End Login Form -->
    </div>
</div>
<!-- End Info Section -->